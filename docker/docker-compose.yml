# @format

# docker-compose.yml

version: "3.8"

services:
  web:
    build:
      context: backend
      dockerfile: Dockerfile
    command: uvicorn app.main:app --host 0.0.0.0
    volumes:
      - .:/app
    ports:
      - 8008:8000
